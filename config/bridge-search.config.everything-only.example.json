{
  "_security_warning": "Copy to config/bridge-search.config.json or set BRIDGE_SEARCH_CONFIG. Keys starting with _ are ignored.",
  "_profile": "Windows filename search via Voidtools Everything (es.exe) only. No AnyTXT HTTP, no WSL locate/find/grep. Use locate_file_or_folder with target_env \"windows\".",
  "_write_note": "manage_file(write) replaces by default. Append requires write_mode='append' on the tool call; there is no config-level append default.",
  "version": 1,
  "service": {
    "anytxt_url": "http://127.0.0.1:9921/search"
  },
  "security": {
    "path_denylist": "default",
    "custom_restricted_prefixes": [],
    "allowed_prefixes": [],
    "allow_grep_from_filesystem_root": false,
    "allow_wsl_locator_from_filesystem_root": false,
    "require_confirm_for_writes": true,
    "require_confirm_for_deletes": true
  },
  "limits": {
    "max_limit": 500,
    "max_offset": 50000,
    "max_depth": 20,
    "max_catalog_lines": 10000,
    "max_locator_results": 5000,
    "anytxt_max_response_bytes": 2097152,
    "command_timeout_seconds": 10
  },
  "backends": {
    "everything": true,
    "anytxt": false,
    "wsl_locate": false,
    "wsl_find": false,
    "wsl_grep": false
  }
}
