{
  "_security_warning": "Copy to config/bridge-search.config.json (or set BRIDGE_SEARCH_CONFIG). Keys starting with _ are ignored. Tighten security before relaxing anything.",
  "_write_note": "manage_file(write) replaces by default. Writes, moves, and copy requests carrying overwrite=true use require_confirm_for_writes; use write_mode='append' only when explicitly intended.",
  "version": 1,
  "service": {
    "anytxt_url": "http://127.0.0.1:9920"
  },
  "security": {
    "path_denylist": "default",
    "custom_restricted_prefixes": [],
    "allowed_prefixes": [],
    "allow_grep_from_filesystem_root": false,
    "allow_wsl_locator_from_filesystem_root": false,
    "require_confirm_for_writes": true,
    "require_confirm_for_deletes": true
  },
  "limits": {
    "max_limit": 500,
    "max_offset": 50000,
    "max_depth": 20,
    "max_catalog_lines": 10000,
    "max_locator_results": 5000,
    "anytxt_max_response_bytes": 2097152,
    "command_timeout_seconds": 10,
    "max_read_bytes": 1048576,
    "max_delete_entries": 1000
  },
  "backends": {
    "everything": true,
    "anytxt": true,
    "wsl_locate": true,
    "wsl_find": true,
    "wsl_grep": true
  }
}
